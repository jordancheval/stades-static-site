{
  "name": "stades-static-site",
  "description": "Site statique d'annuaire de stades",
  "private": true,
  "version": "1.2.0",
  "scripts": {
    "clean:public": "rm -rf ./public",
    "clean:data": "rm -rf ./data",
    "prebuild": "npm run clean:public && npm run clean:data && node ./scripts/TelechargerDonnees.js",
    "build": "npx @11ty/eleventy && npx workbox generateSW workbox-config.js",
    "postbuild": "npm run clean:data",
    "prestart": "npm run prebuild && npx @11ty/eleventy && npx workbox generateSW workbox-config.js",
    "start": "eleventy --serve",
    "debug": "set DEBUG=Eleventy* && npm run build"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.0",
    "dotenv": "^10.0.0",
    "eleventy-plugin-pwa": "^1.0.8",
    "install": "^0.13.0",
    "slugify": "^1.6.5",
    "workbox-cli": "^6.4.2"
  }
}
